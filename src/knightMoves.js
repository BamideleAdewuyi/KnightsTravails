class Traverse {
    constructor(start, finish) {
        this.start = start;
        this.finish = finish;
    };

    possibleMoves(node) {

    };

    calculate(node = this.start) {
        let current = node;
        let q = [node];
        let vis = [node];

    };
};

export default Traverse;